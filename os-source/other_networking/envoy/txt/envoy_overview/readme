All files in this directory are part of the APPN project description.  The
.ps files are postscript files, the .info files are stupid, print is a script
to print out the postscript files, checkin is a script to check in the source
files and all other files are FinalCopy format source files.

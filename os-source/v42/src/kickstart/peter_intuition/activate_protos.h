/* Prototypes for functions defined in
activate.c
 */

BOOL ActivateGadget(struct Gadget * gadget,
                    struct Window * window,
                    struct Requester * request);

int IActivateGadget(struct InputToken * it);


This is the source to the inet:serv/timed server. This shows just how to
write a server that gets launched by inetd. Note the use of the s_inherit()
function. Inetd uses s_release() to hand off the socket in question. See 
the autodocs on these functions for examples.

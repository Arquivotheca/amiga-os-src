count /ssize exch def


/page {
	gsave
	10 10 moveto
	/Courier findfont 40 scalefont setfont

	(Courier ) show			% sentence is broken up to prove CP advance works
	(Fixed-Width ) show
	(FONT) show
	
	/Times-Roman findfont 50 scalefont setfont

	gsave
	345 340 moveto
	{pop pop -15 rotate (-) show} bind (kshow!) kshow
	grestore	

	/ang 0 def
	0 .1 1.0 { gsave setgray
		270 200 moveto
		ang rotate /ang ang 36 add def
		(Times) show
		grestore} bind for
	
	/Times-Roman findfont 180 scalefont setfont
	400 100 moveto
	30 setlinewidth
	(&) show

	20 setlinewidth
	($) true charpath
	0 setlinewidth
	stroke
	
	1 setlinewidth 10 50 moveto
	/Helvetica findfont 10 scalefont setfont
	13 { 
		1.1 1.1 scale
		(Helvetica) show
		-40 13 rmoveto
		} bind repeat

	grestore

	gsave
		[55] 25 setdash
		5 5 moveto 635 5 lineto 635 395 lineto 5 395 lineto closepath
		stroke
	grestore
} bind def
%%-------------------------------------------------------------------
0.2 setflat

gsave
5 5 translate
.5 .95 scale
page
grestore
%%-------------------------------------------------------------------
gsave
335 200 translate
-45 rotate
.4 .4 scale
page
grestore
%%-------------------------------------------------------------------
gsave
500 282 translate
30 rotate
.17 .17 scale
%page
grestore
%%-------------------------------------------------------------------

/Times-Roman findfont 20 scalefont setfont

335 100 moveto
(Normal) show
335 70 moveto
8 0 65 (SpeciAl) widthshow
335 40 moveto
10 0 (Global) ashow

%%-------------------------------------------------------------------
gsave
350 350 translate
.6 .6 scale

%% This file is a copy of the example Helvetica Metrics changing program of
%% section 5.7 in Adobe's Red Book

% clippath stroke

/Helvetica findfont
dup length 2 add dict /newdict exch def
	{ 1 index /FID ne
		{newdict 3 1 roll put}
		{pop pop}
	 ifelse
} forall

10 dict begin
[/zero /one /two /three /four /five /six /seven /eight /nine]
{ [250 0 750 0] def } forall
newdict /Metrics currentdict put
end

/MyHelv newdict definefont pop

/MyHelv findfont 50 scalefont setfont
0 0 moveto
{pop pop gsave 0 -40 rlineto stroke grestore} (abc01234abc) kshow

grestore
%%-------------------------------------------------------------------

count ssize ne {
	(Bug !!! Operand stack has grown\n) print
	pstack
	} if

These files were created by running FixFonts while having FONTS: assigned to a
directory containing all the distribution fonts. Redo these files if any of the
fonts shipped with Workbench are changed.
.

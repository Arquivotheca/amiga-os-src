(autodocs.c: 39.12)
Minor clarifications to ScreenDepth(), ScreenPosition()

(coercion.c: 38.16)
SpriteFactor.Y is now exaggerated by a factor of two, to allow scaling
down, too.

(gadgetclass.h: 38.6)
gpInput structure now has gpi_TabletData field.

(gadgetclass.i: 38.5)
gpInput structure now has gpi_TabletData field.

(intuition.h: 38.17)
Clarified ColorSpec doc.

(intuition.i: 38.15)
Clarified ColorSpec doc.

(isdefault.c: 38.15)
sendWindowMouse() sends message for every tablet event, even if mouse
position is unchanged.

(isupport.c: 38.12)
Fixed my part of the "booting in PAL" problem.  Was losing MACHINE_ISPAL
flag.

(pointer.c: 39.14)
Enabled sprite-scan doubling.  Now calculates sprite position after
changing sprite.

(pointerclass.h: 39.4)
Noted that bitplane data need not be in chip.

(pointerclass.i: 39.3)
Noted that bitplane data need not be in chip.

(sc.h: 38.21)
SpriteFactor.Y is now exaggerated by a factor of two, to allow scaling
down, too.

(screens.h: 38.21)
Fixed up comments surrounding SA_DisplayID, DrawInfo and SDEPTH_CHILDONLY.

(screens.i: 38.21)
Fixed up comments surrounding SA_DisplayID, DrawInfo and SDEPTH_CHILDONLY.

(sgadget.c: 38.17)
Now passes TabletData to GM_GOACTIVE and GM_HANDLEINPUT.

(view.c: 38.20)
Any hidden viewport now has PSF_REMAKEVPORT set, so we're sure to
remake when its becomes visible again.  updateMousePointer() is now
called with the current mouse pointer (instead of NULL), so that the
sprite speed/height can be recalculated.

(windows.c: 38.29)
Removed extraneous parameter to FreeVec().
